# 프로젝트 기억 공유 서비스 사용법

## 1. 계정과 첫 프로젝트

`bash scripts/setup.sh` → `npm run dev` → `http://localhost:5173`을 엽니다. **회원가입**으로 개별 계정을 만드세요. 사용자 이름은 영문/숫자/`_.-` 3~64자, 비밀번호는 12자 이상입니다. 한 번 표시되는 복구 코드를 안전한 곳에 보관하세요. 현재 계정 복구는 이 코드로 하며 이메일 발송 기능은 없습니다.

**프로젝트 · 인수인계**에서 파일이나 폴더를 가져오면 자동으로 정리됩니다. 빈 폴더를 직접 만들 때는 **새 폴더**를 여세요. 로컬 경로 입력은 필요하지 않습니다.

`데모 시작하기`는 공통 예시 계정입니다. 다른 사람과 실제로 공유할 때는 각자의 계정으로 가입해야 합니다. 운영 구성은 데모 로그인을 비활성화합니다.

## 2. WSL 기억을 가져오고 분류하기

설치한 저장소의 WSL 터미널에서:

```bash
npm run memory:export -- --output ~/passport-import.json --project /home/me/my-project
# 대화의 user/assistant 텍스트도 포함하려면:
npm run memory:export -- --output ~/passport-import-sessions.json --project /home/me/my-project --include-sessions
```

`--project`는 여러 번 지정할 수 있습니다. 기본 경로는 `$CODEX_HOME` 또는 `~/.codex`, `$CLAUDE_CONFIG_DIR` 또는 `~/.claude`입니다. 지원 입력:

- Codex `memories/`의 Markdown, `sessions/` JSONL의 프로젝트 메타데이터 및 선택한 user/assistant 텍스트.
- Claude `projects/*/memory/` Markdown, 프로젝트 JSONL의 작업 경로 및 선택한 user/assistant 텍스트.
- 명시한 프로젝트 최상위 `MEMORY.md`, `AGENTS.md`, `CLAUDE.md`.

자격증명·설정·도구 결과·시스템 프롬프트·추론 기록은 수집 대상이 아닙니다. 심볼릭 링크를 따라가지 않습니다. 출력은 권한 600인 로컬 JSON이며 **자동 업로드하지 않습니다**. 이미 있는 출력 파일은 덮어쓰지 않습니다. 큰 파일/세션에는 크기 제한이 있으며, 지원하지 않는 저장 형식을 임의로 추측하지 않습니다. 자동 비밀정보 제거는 완전하지 않으므로 선택한 내용은 직접 확인하세요.

GUI에서 **파일 가져오기** 또는 **폴더 가져오기**를 선택하면 내용을 분석해 프로젝트·주제별 폴더에 바로 저장합니다. 항목 선택·분류 폴더 생성·저장·개별 승인을 따로 누르지 않습니다. 새 폴더는 비공개이며 이미 공유한 폴더에 추가하는 자료는 해당 폴더 권한을 따릅니다.

제목·전체 본문·프로젝트 정보의 단어 특징과 유사도를 사용해 분류합니다. 외부 AI 제공자로 원문을 보내지 않습니다. 분류가 틀렸으면 기억의 폴더 이동 기능으로 수정하세요. 같은 폴더의 동일 내용은 한 번만 가져오고, 비슷하지만 다른 내용이나 긴 파일의 조각은 삭제·요약하지 않습니다. 일부 저장 실패 시 남은 항목을 재시도할 수 있습니다. 지속 감시/자동 동기화 기능은 아닙니다.

## 3. GUI에서 정리하기

- 폴더 이름·인수인계 문서 수정, 하위 폴더 생성, 폴더 이동/삭제.
- 기억 검색·편집·종류 지정·버전 이력 확인·폴더 간 이동/삭제.
- 기존 Passport 기억을 별도 승인 없이 새 폴더로 복사.
- 폴더 JSON 내보내기, 요금제 화면에서 계정 데이터 내보내기/탈퇴.

인수인계 문서에는 배경·진행 상황·결정 근거·다음 작업·막힌 점을 정리하세요. 내용 변경은 버전으로 검사하므로 다른 사람이 먼저 수정했으면 재조회 후 변경 내용을 반영해야 합니다. 폴더의 기억을 이동하면 대상 폴더의 공유 권한이 적용됩니다. 폴더 삭제는 포함한 기억·이력·공유를 삭제하며, 하위 폴더는 먼저 이동하거나 삭제해야 합니다.

## 4. 다른 작업자에게 인수인계하기

양쪽 PC에서 같은 서버에 각자의 계정으로 로그인합니다. 소유자는 원하는 폴더의 **공유 설정**에서 `읽기` 또는 `편집`을 선택하고 **공유 코드 발급**을 누릅니다. 받는 작업자는 **일회용 코드로 공유받기**에 받은 12자리 코드를 입력합니다. 소유자는 표시된 상대 계정 이름을 확인한 뒤 **보내는 PC 코드 확인**에 같은 코드를 입력하고 **상대 확인 후 공유 연결**을 누릅니다. 양쪽 확인이 끝나야 접근 권한이 생깁니다.

코드는 10분간 한 번만 사용할 수 있으며 취소·재발급 시 이전 대기 요청은 무효화됩니다. 코드 만료는 연결 대기에만 적용되며 완료된 공유는 별도로 철회할 때까지 유지됩니다. 이전 초대 링크와 계정 ID 직접 신규 공유는 더 이상 지원하지 않습니다. 코드 원문은 DB에 저장하지 않습니다. 서버의 시도 제한은 재시작 후에도 유지됩니다. 앱이 이메일이나 메시지를 대신 보내지는 않습니다.


- 읽기: 문서·기억·이력 조회와 내보내기.
- 편집: 문서·기억 작성/편집, 가져오기와 검토. 소유권 변경·재공유·폴더 이동/삭제는 불가.
- 소유자: 폴더 관리와 공유/철회.

공유는 지정한 폴더에만 적용되며 하위 폴더로 자동 상속되지 않습니다. 소유자의 로컬 파일 경로와 원문 출처 경로는 다른 작업자에게 표시하지 않습니다. 철회 후 새 요청은 차단되고 해당 사용자의 에이전트 허용과 경로 연결도 제거됩니다. 이미 내려받은 사본이나 읽은 내용을 회수하는 기능은 아닙니다.

## 5. Claude·Codex 동시 협업

같은 폴더의 **함께 작업하기**에서 작업 제목·설명·작업 영역을 등록합니다. 예를 들어 Claude는 `apps/web`, Codex는 `apps/api`를 맡습니다. 비어 있는 영역은 프로젝트 전체를 뜻합니다. 현재 작업·담당자·진행 보고·완료 상태가 5초마다 갱신되며, 이전 진행 보고는 이력에서 확인합니다.

**Claude·Codex 협업 연결**을 열고 각 도구에 별도의 연결 키를 발급합니다. 같은 토큰을 두 도구가 사용하면 서버가 담당자를 구분할 수 없습니다. 각 연결은 해당 폴더의 읽기+쓰기 권한이 필요합니다. 공유받은 읽기 전용 사용자는 읽기만 연결할 수 있습니다.

```bash
read -rs PASSPORT_AGENT_TOKEN
export PASSPORT_AGENT_TOKEN
npm run client:setup -- --provider claude
unset PASSPORT_AGENT_TOKEN
# Codex는 별도로 발급한 키로 --provider codex를 실행
```

서버 URL을 입력하면 `.data/service-client-claude.json` 또는 `service-client-codex.json`에 권한 600으로 보관합니다. 원격 서버는 HTTPS만 허용합니다. 기존 전역 설정은 변경하지 않습니다. 이전 공용 `service-client.json`은 호환용으로 읽되 협업에는 별도 연결을 사용하세요.

프로젝트 위치에서 `node /설치경로/agent-passport/scripts/launch-client.mjs claude`와 `codex`를 각각 실행하고, 폴더 화면의 작업 지침을 전달합니다. 지침에 폴더 ID가 포함되므로 로컬 경로 바인딩 없이 사용할 수 있습니다.

협업 도구:

- `get_folder_tasks`: 최신 작업·담당자·진행 보고 읽기.
- `create_folder_task`: 작업과 상대 작업 영역 등록.
- `claim_folder_task`: 최신 revision으로 담당 요청. 같은 작업/겹치는 영역은 동시에 맡을 수 없습니다.
- `update_folder_task`: 진행 공유, 도움 요청(`blocked`), 완료(`done`). `active` 보고 시 담당 시간을 30분 연장합니다.
- `release_folder_task`: 다른 에이전트가 이어받도록 담당 해제.
- `get_folder_task_history`: 과거 진행 보고/인수인계 읽기. 페이지당 100개.

폴더 컨텍스트에도 최근 작업 20개와 짧은 진행 보고가 포함됩니다. 전체 목록은 `get_folder_tasks`로 읽습니다. 작업은 폴더당 200개까지 보관하며 완료/대기 작업은 GUI에서 삭제할 수 있습니다. 설명·진행·이력은 암호화하여 서버에 저장하고, 폴더 내보내기와 사용량 집계에도 포함합니다.

담당 만료나 갱신 실패 시 에이전트는 수정을 멈추고 최신 상태를 다시 읽어야 합니다. 이 기능은 서버상의 작업 조정이며 OS 파일 잠금이 아닙니다. 서로 다른 작업 영역과 worktree를 사용하고 병합 전에 검토하세요. 에이전트를 자동 실행하거나 전체 채팅을 실시간 수집하는 기능은 아닙니다.

기존 `get_folder_context`, `search_folder`, `propose_folder_memory`와 8개 기억 도구도 유지됩니다(총 17개). 진행 보고는 팀에 즉시 공유되지만 장기 기억 제안은 기존처럼 사람의 승인이 필요합니다. 공유/에이전트 권한을 철회하면 다음 읽기·쓰기가 차단됩니다.

## 6. 요금제와 데이터 보관

현재 Free/Pro 한도는 운영자가 환경변수로 정하는 개발 기본값이며 최종 판매 가격이 아닙니다. 폴더 수, 기억 수, 암호화 본문·이력·대기 제안 저장량, 폴더 공유 건수를 서버에서 계산합니다. 팀 폴더에 다른 작업자가 쓴 데이터는 폴더 소유자의 사용량에 포함됩니다. 한도 초과 쓰기는 트랜잭션 전체를 롤백합니다. 다운그레이드 후에도 읽기/내보내기/삭제가 가능합니다.

Stripe 어댑터는 HTTPS origin과 키·가격 ID·웹훅 설정이 모두 있어야 활성화됩니다. 결제 반환 URL만으로 Pro를 부여하지 않으며 공급자의 현재 구독 상태를 확인합니다. 매시간 재확인을 시도하고, 72시간 이상 검증되지 않은 유료 상태는 Free 한도로 처리합니다. 구독 관리는 공급자 포털에서 합니다. 공급자는 교체할 수 있지만 다른 공급자용 어댑터는 별도 구현이 필요합니다.

탈퇴는 내 소유 데이터·세션·에이전트·공유를 삭제합니다. 다른 사람 소유 폴더에 작성한 협업 문서와 결제 공급자에 보관된 정보는 별개입니다. 운영 DB의 삭제와 백업 보존 기간은 별개이므로 운영자는 배포 문서에 따라 백업 보존 정책을 설정해야 합니다.

## 참고한 형식과 연결 문서

[Codex MCP](https://developers.openai.com/codex/mcp/) · [Codex 고급 설정](https://developers.openai.com/codex/config-advanced/) · [Claude Code 기억](https://code.claude.com/docs/en/memory) · [Stripe 구독 Checkout](https://docs.stripe.com/payments/checkout/build-subscriptions) · [Stripe 웹훅](https://docs.stripe.com/webhooks).

JSONL 파서는 이 환경의 로컬 구조와 합성 fixture로 검증했습니다. 모든 향후 CLI 저장 형식에 대한 호환성을 보장하지 않습니다.

## 폴더와 작업 타임라인

내 폴더는 제목과 폴더 아이콘 중심의 격자로 표시합니다. 원본 경로에서 확인된 Claude 자료는 테라코타, Codex 자료는 검정·회색으로 표시하며, 혼합 폴더는 두 색과 두 아이콘을 함께 표시합니다. 출처를 확인할 수 없는 폴더는 중립색입니다. 폴더를 클릭하면 별도의 상세 화면으로 이동합니다. 점 세 개 메뉴에서 공유 또는 이름 변경·관리를 열 수 있고, 내 폴더 버튼으로 목록에 돌아갑니다. 기억 가져오기와 새 폴더 만들기는 목록 화면에 있습니다.

상세 화면은 원문의 날짜 기준으로 작업 기록을 시간순 정렬합니다. 최신순으로 전환할 수 있습니다. 날짜마다 작업 보고·결정·다음 작업·사용자 요청을 구분한 짧은 미리보기가 나오며, 클릭하면 자세한 기록과 원문을 확인합니다. 기술 메타데이터·파일 경로·코드 블록은 요약에서 제외합니다. 원문은 보존합니다.

요약은 원문 문장과 구조를 바탕으로 정리하는 방식이며 AI 종합 판단은 아닙니다. 요청을 완료로 바꾸지 않고, 기억 문서의 구성 설명은 당시 기록된 상태로 표시합니다. 대화 시각은 한국 날짜 기준, 날짜 없는 기록은 날짜 미상입니다. 가져온 날짜를 작업 날짜로 사용하지 않습니다. 개별 날짜의 추가 내용은 30개씩 펼쳐 봅니다.

### 긴 대화 전체 내보내기

`npm run conversation:export -- --output /private/conversations.json`은 Claude projects와 Codex sessions/archived_sessions의 사용자·어시스턴트 텍스트를 끝까지 읽습니다. 다른 설치는 `--codex-home PATH`, `--claude-home PATH`로 지정합니다. 원본 파일은 수정하지 않으며 서버에 자동 업로드하지 않습니다. 긴 대화는 30,000자 항목으로 나눕니다. 도구 결과·내부 추론·이미지 바이트는 포함하지 않고 알려진 비밀정보 패턴을 가립니다. 손상된 JSON 줄 수는 출력 파일의 `sources.malformed`에 기록합니다. GUI의 파일 용량/항목 수 제한보다 크면 나눠 가져와야 합니다.

모든 원문은 별도로 페이지를 넘겨 조회할 수 있습니다.
